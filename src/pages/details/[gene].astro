---
import Layout from "@/layouts/Layout.astro";

export async function getStaticPaths() {
  return [{ params: { gene: "TC013553" } }];
}

const { gene } = Astro.params;
---

<Layout> Details of gene {gene}</Layout>
